{
  "name": "tdd-basic-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "jest": "^29.7.0"
  }
}
